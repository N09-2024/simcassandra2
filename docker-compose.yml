services:
  cassandra-node1:
    image: cassandra:4.1
    container_name: cass-node1
    environment:
      - CASSANDRA_CLUSTER_NAME=SimCluster
      - CASSANDRA_SEEDS=cass-node1
      - HEAP_NEWSIZE=128M
      - MAX_HEAP_SIZE=512M
    ports:
      - "9042:9042"
    networks:
      - cassandra-net
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'describe keyspaces' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 15
      start_period: 60s

  cassandra-node2:
    image: cassandra:4.1
    container_name: cass-node2
    environment:
      - CASSANDRA_CLUSTER_NAME=SimCluster
      - CASSANDRA_SEEDS=cass-node1
      - HEAP_NEWSIZE=128M
      - MAX_HEAP_SIZE=512M
    networks:
      - cassandra-net
    depends_on:
      cassandra-node1:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'describe keyspaces' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 15
      start_period: 60s

  cassandra-node3:
    image: cassandra:4.1
    container_name: cass-node3
    environment:
      - CASSANDRA_CLUSTER_NAME=SimCluster
      - CASSANDRA_SEEDS=cass-node1
      - HEAP_NEWSIZE=128M
      - MAX_HEAP_SIZE=512M
    networks:
      - cassandra-net
    depends_on:
      cassandra-node2:
        condition: service_healthy

networks:
  cassandra-net:
    driver: bridge